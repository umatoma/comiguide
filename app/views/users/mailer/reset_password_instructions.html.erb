<p><%= @resource.username %> 様</p>

<p>下記リンクからパスワードを再設定することが出来ます。</p>

<p><%= link_to 'パスワード再設定', edit_password_url(@resource, reset_password_token: @token) %></p>

<%= render 'share/mailer/footer' %>
